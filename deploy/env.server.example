# 服务器构建环境（复制为 deploy/env.server，勿提交含密钥的版本）
# server-deploy.sh 会自动 source 此文件

VITE_BASE=/

# API 子域名
VITE_API_BASE_URL=http://api.dropvid.cn
# 上 HTTPS 后改为：https://api.dropvid.cn

# 构建前同步 platforms/limits（本机 API，走 127.0.0.1 最快）
DROPVID_API_BASE=http://127.0.0.1:3000
